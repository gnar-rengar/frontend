import React, { Suspense } from 'react';
import Fit from '../../components/recommend/Fit';

function FitPage() {
  return (
    <Suspense>
      <Fit />
    </Suspense>
  );
}

export default FitPage;
