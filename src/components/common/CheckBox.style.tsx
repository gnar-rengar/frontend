import styled from '@emotion/styled';

export const Input = styled.input`
  position: relative;
  top: 0;
  left: 0;
  width: 30px;
  height: 30px;
  margin: 0;
`;

export const Label = styled.label`
  position: relative;
  display: inline-flex;
  width: min-content;
`;
